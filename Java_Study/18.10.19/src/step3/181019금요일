*상속
1. why?
	부모의 멤버,(변수, 메서드)를 물려받아 개발의 생산성(재사용성)을 높임
	계층구조를 형성  

2. 특징
	자바는 단일 상속을 채택(다중 상속의 장점은 Interface로 차용)
	extends 를 사용해 상속
	is a relationship (사람은  < 동물이다. 비서는  < 사원이다.)
	부모의 private 멤버에 접근 불가능 -> set/get 으로 접근가능

3. 구현 
	부모(Super) / 자식(Sub)
	
	class Sub extends Super {
	 //....
	}
	
		